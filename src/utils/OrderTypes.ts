export type CreateOrderParams = {
  orderNo: string;
  deliverDate: Date;
  supplierstatus: string;
  managerstatus: string;
  createdAt: Date;
};

export type UpdateOrderParams = {
  orderNo: string;
  deliverDate: Date;
  supplierstatus: string;
  managerstatus: string;
  createdAt: Date;
};
