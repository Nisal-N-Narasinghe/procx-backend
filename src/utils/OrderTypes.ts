export type CreateOrderParams = {
  orderNo: string;
  deliverDate: Date;
  supplierstatus: string;
  managerstatus: string;
  orderStatus: string;
  createdAt: Date;
  orderStatus: string;
};

export type UpdateOrderParams = {
  orderNo: string;
  deliverDate: Date;
  supplierstatus: string;
  managerstatus: string;
  orderStatus: string;
  createdAt: Date;
  orderStatus: string;
};
