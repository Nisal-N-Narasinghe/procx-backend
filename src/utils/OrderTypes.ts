export type CreateOrderParams = {
  orderNo: string;
  deliverDate: Date;
  supllier: string;
  item: string;
  qty: number;
};

export type UpdateOrderParams = {
  orderNo: string;
  deliverDate: Date;
  supllier: string;
  item: string;
  qty: number;
};
