export type CreateItemParams = {
  itemName: string;
  restricted: boolean;
  description: string;
  imageUrl: string;
};

export type UpdateItemParams = {
  itemName: string;
  restricted: boolean;
  description: string;
  imageUrl: string;
};
